@model IEnumerable<WebForSell.Models.LastestProduct>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LastestProductName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastestProductPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastestProductImage)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LastestProductName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastestProductPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastestProductImage)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LastestProductId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LastestProductId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LastestProductId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
